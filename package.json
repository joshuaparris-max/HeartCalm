{
  "name": "heart-calm",
  "version": "1.0.0",
  "private": true,
  "description": "Calm — a breathing & palpitation support PWA. Not a medical device.",
  "type": "module",
  "scripts": {
    "dev": "http-server . -p 8080 -c-1",
    "test": "node --test",
    "test:e2e": "playwright test"
  },
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "http-server": "^14.1.1"
  }
}
